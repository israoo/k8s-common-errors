apiVersion: v1
kind: Pod
metadata:
  name: insufficient-resources
spec:
  containers:
    - name: app
      image: nginx:latest
      resources:
        requests:
          memory: "64Mi"
          cpu: "250m"
